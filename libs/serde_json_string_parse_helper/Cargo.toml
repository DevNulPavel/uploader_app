[package]
name = "serde_json_string_parse_helper"
version = "0.1.0"
authors = ["Pavel Ershov <pershov@game-insight.com>"]
edition = "2021"

[dependencies]
serde = "1"
serde_json = "1"

[dev-dependencies]
serde = {version="1", features=["derive"]}

[package]
name = "json_parse_helper"
version = "1.0.0"
authors = ["Pavel Ershov <pershov@game-insight.com>"]
edition = "2021"

[dependencies]
serde = {version="1", features=["derive"]}
serde_json = "1"

[dev-dependencies]
